spring:
  application:
    name: simvex
  profiles:
    active: secret
    include: oauth
  datasource:
    url: jdbc:mysql://localhost:3306/simvex
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
  jwt:
    secret: ${JWT_KEY}
    access-token-expiration-ms: 3600000 # 1시간 (ms 단위)
